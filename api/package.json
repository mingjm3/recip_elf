{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm run build && node ./bin/www",
    "build": "pushd ../frontend && npm run build && popd"
  },
  "dependencies": {
    "@prisma/client": "^4.16.2",
    "argon2": "^0.30.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "~4.16.1",
    "jsonwebtoken": "^9.0.1",
    "morgan": "~1.9.1",
    "openai": "^3.3.0",
    "winston": "^3.10.0"
  },
  "devDependencies": {
    "prisma": "^4.16.2"
  }
}
